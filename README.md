# File Server v1.0

Современный веб-файловый сервер на Flask с личными кабинетами, общими папками и удобным интерфейсом в стиле настольного проводника.

## Установка

``` bash
git clone https://github.com/Lentayi/file-server.git
cd file-server
pip install -r requirements.txt
```

## Если admin не создан
Запустите скрипт 
``` bash
python create_admin.py
```

## Настройки через `.env`

Основные настройки хранилища задаются в файле `.env` в корне проекта:

``` env
FILE_SERVER_STORAGE_PATH=/home/admin/file-server/uploads
FILE_SERVER_STORAGE_QUOTA=10GB
FILE_SERVER_UPLOAD_RAM_BUFFER=1GB
```

- `FILE_SERVER_STORAGE_PATH` — папка, где будут храниться загруженные файлы. Можно указать абсолютный путь или путь относительно проекта.
- `FILE_SERVER_STORAGE_QUOTA` — общий лимит хранилища. Поддерживаются значения вроде `500MB`, `10GB`, `1.5TB` или число байт.
- `FILE_SERVER_UPLOAD_RAM_BUFFER` — сколько оперативной памяти можно использовать как буфер для быстрых загрузок. Если файл меньше этого лимита и в буфере есть место, сервер сначала принимает его в RAM, а потом фоново записывает на диск. `0` отключает RAM-буфер.

После изменения `.env` перезапустите сервер.



## Возможности

- авторизация пользователей
- личное хранилище для каждого пользователя
- общие папки с настройкой доступа
- загрузка, создание папок, переименование, копирование, вырезание, вставка и удаление файлов
- отдельный просмотрщик всех файлов для администратора
- управление пользователями через админ-панель
- выдача ролей и прав доступа
- смена пароля и аватара
- светлая и темная тема
- автоматическое переключение темы по времени суток
- аккуратный дашборд с информацией о хранилище и быстрым переходом к разделам

## Роли

### Пользователь
- доступ к личному хранилищу
- просмотр доступных общих папок
- смена пароля
- смена аватара
- просмотр своей роли и прав

### Администратор
- все возможности обычного пользователя
- создание новых пользователей
- выдача прав администратора
- управление доступом к общим папкам
- создание и изменение общих папок
- просмотр всех файлов и папок на сервере, включая личные хранилища пользователей

## Технологии

- Python
- Flask
- Flask-Login
- Flask-SQLAlchemy
- SQLAlchemy
- Werkzeug
- HTML / CSS / Jinja2
- SQLite

## Статус

Релиз `v1.0`
Проект готов к базовому использованию и дальнейшему развитию.
